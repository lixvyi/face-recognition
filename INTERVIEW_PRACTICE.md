# 面试实践题：HRI Demo 感知能力优化

## 背景

这是一个 HRI（Human-Robot Interaction）demo。系统会通过摄像头、麦克风、视觉语言模型和本地模型，推断用户状态、场景物体和人-物交互关系。

目前项目里有两个明显需要优化的方向。候选人可以任选其一完成，也可以两个都做。我们更关注候选人如何分析问题、设计方案、验证效果，而不是照着固定步骤实现。

## 方向 A：优化情绪判别能力

### 问题

当前情绪识别模块已经接入了面部和语音两路信号，但整体效果还不够理想：

- 情绪结果不够实时，用户状态变化后反馈有延迟。
- 面部情绪识别不够稳定，容易受角度、光照、眼镜、裁剪质量影响。
- 语音情绪识别有时会 fallback，采样、解码、模型加载和超时都可能影响结果。
- 模型输出类别较少，表达能力有限。
- 面部和语音融合策略比较粗糙，可能过度相信某一路信号。
- UI 容易让用户误以为系统“准确读懂了真实情绪”，但这其实只能作为弱状态信号。

### 目标

请改进当前情绪判别链路，使它更实时、更稳定、更可信。

你可以选择优化其中一个或多个方面：

- 面部情绪识别的稳定性和实时性。
- 语音情绪识别的稳定性和响应速度。
- 面部与语音的融合策略。
- 情绪不确定性、低置信度和 fallback 的表达方式。
- 更合适的模型、特征或后处理策略。

### 期望结果

完成后需要能说明：

- 当前情绪链路的主要问题是什么。
- 你选择优化哪一部分，为什么。
- 你的方案如何减少延迟、减少 fallback 或提升稳定性。
- 你的方案如何避免把情绪识别表达成心理诊断。
- 你如何验证优化有效。

### 验收关注点

我们会重点看：

- 是否定位到真实瓶颈，而不是只改 UI 文案。
- 是否有清晰的 real / fallback / sampling / timeout 状态处理。
- 是否能处理模型低置信度和不确定性。
- 是否能量化或至少清楚展示优化前后的差异。
- 是否能保持用户体验克制、不过度推断用户情绪。

## 方向 B：优化 VLM bbox 物体图像证据能力

### 问题

当前 VLM 可以识别场景物体并返回 bbox，但用 bbox 裁剪出来的物体缩略图经常不对应真实物体。这会导致关系库里的图像证据不可信。

可能的问题包括：

- VLM 输入是多帧，bbox 到底对应哪一帧并不总是可靠。
- bbox 本身可能不准。
- bbox 格式可能被模型写错，例如把 `[x, y, w, h]` 写成 `[x1, y1, x2, y2]`。
- 小物体、手持物体、遮挡物体尤其容易裁错。
- 错误缩略图一旦保存，会污染后续关系库。
- 当前缺少足够的 debug 信息来判断为什么截错。

### 目标

请改进 VLM 物体图像证据生成链路，使关系库中保存的物体缩略图更可信。

核心要求不是机械服从某个字段，而是：

> 最终保存的缩略图应该真实对应被识别的物体，不能明显截到别的物体、背景或错误区域。

你可以自由设计方案，例如：

- 多帧候选裁剪。
- bbox 格式兼容和异常检测。
- bbox padding 或重裁剪策略。
- 对低可信 bbox 标记 uncertain。
- 增加 debug 信息帮助定位错图原因。
- 改进 VLM prompt 或返回格式。
- 清理或迁移旧的错误缓存。

### 期望结果

完成后需要能说明：

- 当前 bbox 截图错误的主要原因是什么。
- 你的方案如何选择正确的图像证据。
- 你的方案如何处理 bbox 不准、帧不准或格式不准。
- 错误图像如何被发现、避免或清理。
- 你如何验证缩略图确实对应物体。

### 验收关注点

我们会重点看：

- 关系库中的物体缩略图是否明显更准确。
- 是否避免固定使用某一帧导致系统性错图。
- 是否能处理小物体、手持物体和遮挡物体。
- 是否有调试手段解释某张图为什么被保存。
- 是否能处理历史错误缓存。
- 是否能区分 VLM 识别错误和前端裁剪错误。

## 通用要求

无论选择哪个方向，都需要提交：

1. 你对问题的分析。
2. 你的设计方案和取舍。
3. 代码改动。
4. 验证方法和结果。
5. 已知限制。

不要求一次性解决所有问题。我们更看重：

- 是否能抓住关键问题。
- 是否能提出合理工程方案。
- 是否能验证方案有效。
- 是否能清楚说明限制和下一步。

## 评分重点

- 问题定位是否准确。
- 方案是否符合当前项目约束。
- 实现是否稳定、可维护。
- 是否有可复现的验证方法。
- 是否避免过度承诺模型能力。
- 是否能在不确定场景下给出合理 fallback。

